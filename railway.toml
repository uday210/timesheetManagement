[build]
builder = "nixpacks"

[build.nixpacks]
NODE_VERSION = "22"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
