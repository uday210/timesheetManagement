[build]
builder = "nixpacks"
# Node version is pinned via .nvmrc and package.json "engines" (Nixpacks reads those).

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
